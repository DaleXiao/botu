# botu .impeccable.md — Visual design authority

> 本文件是 botu 视觉语言的**唯一事实源（SoT）**。
> 任何与本文件冲突的 UI 改动一律算输；要改视觉，先改本文件。
> 状态: **正式版**（T-737 W5 草案经 SPEC-437 / T-738 审定落地，2026-09-22）。
> 来源: 提炼 css/style.css + index.html + js/app.js 现有实现，**不发明新风格**；审计发现项（tmp/botu-ui-audit.md）已全部随 SPEC-437 修复并以最终值成文。

## 1. Positioning

**免费、免注册的单一用途工具**：把任意角色图变成极简 bot icon。Mode = **Operate**（用户来完成任务），画廊兼作 Persuade 证据。
界面安静退后，主角永远是用户的 before→after 图对。每个元素必须回答：「它帮用户上传、生成或下载了吗？」没有就删。
视觉语言：**近黑 dark 默认 + 柔和 light 派生**，系统无衬线，单一蓝 accent + 单一暖金辅色，大留白，细线条 SVG 图标。零外部字体、零框架、零构建。

## 2. Typography (hard rules)

- 唯一字体栈: `system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "PingFang SC", "Microsoft YaHei", sans-serif`。**禁止引入外部 webfont**（免费轻量是产品承诺的一部分）。
- body 15.5px / 1.7，letter-spacing 0.01em；移动端 ≤560px 降 14.5px。
- 字阶（只允许这些档位）:

| 档 | 尺寸/字重 | 用途 |
|---|---|---|
| tagline | 1.5rem/600（移动 1.25rem） | hero 主标语 |
| section | 1.15rem/600 | gallery 标题 |
| card-title | 1.02rem/600 | dropzone 标题 |
| body | 0.93-0.95rem/400 | 按钮、副文案 |
| small | 0.85-0.9rem/400 | hints、quota、footer |
| micro | 0.78-0.82rem/400 | meta、figcaption、pill |

- 所有数字读数（elapsed/文件大小/quota/gallery 序号）必须 `font-variant-numeric: tabular-nums`。
- 禁衬线体、禁装饰斜体、禁全大写标题。

## 3. Color (hard rules)

**dark 是主视觉（无存储偏好时默认 dark，follow prefers-color-scheme）；light 是同一语言的派生。**

| token | dark | light |
|---|---|---|
| --bg | #161719 | #fafaf8 |
| --panel | #1d1e21 | #fdfdfc |
| --panel2 | #131416 | #f4f4f1 |
| --ink | #c8cace | #3a3d42 |
| --bright | #e8e9ec | #23262a |
| --muted | #85888e | #6f747b |
| --accent | #7fa6e8 | #3d6db5（AA 4.96 on-bg / 5.00 ink-on-it） |
| --accent2 | #c9a86a | #8a6d35（AA 4.65/4.70） |
| --accent-ink | #14161a | #fbfbfa |
| --line | #2a2c30 | #e4e4df |
| --err-bg / --err-ink | #2a1c1e / #e08585 | #fbf0ef / #b04a44 |

- 全站只有两个彩色: --accent（蓝 = 交互主色: hover/focus/quota/primary 按钮）+ --accent2（暖金 = 仅 quota 耗尽与 gallery 序号两处语义点缀）。**禁止第三色相。**
- **对比度硬规则**: accent 作文本或作按钮底时与对应背景 ≥4.5:1（WCAG AA）；任何 token 变更必须双主题重算（dark 过 ≠ light 过，SPEC-437 P1-1 教训：light accent 系曾在 4.05/4.09/3.81 三处未达）。
- 无渐变、无 glassmorphism/backdrop-blur、无 glow 叠加。层级靠明度: 页 --bg / 卡 --panel / 下沉 --panel2。
- ::selection 与 dropzone hover/dragover tint 必须跟随 accent token：`color-mix(in srgb, var(--accent) N%, transparent)`（selection 28% / dropzone 5%，SPEC-437 已落地）。**禁 rgba 字面量。**
- 阴影只一档 --shadow（dark `0 1px 3px rgba(0,0,0,.2)` / light `rgba(30,32,36,.05)`），仅 .card 用。

## 4. Space & shape

- 壳层宽度三档: 页面 max-width **860** / 工作卡 **680** / dropzone 与状态行 **620**，全部水平居中；main padding `8px 24px 56px`。
- **圆角四档（固化，禁止新增档位）**: 容器卡片 **12px**（--radius）/ 图片 **10px** / 控件按钮 **8px** / pill **999px**。
- 边框: `1px solid var(--line)`；虚线边框仅 dropzone。交互 hover = 边框转 --accent，不改布局不位移。
- 卡片内边距 28px（work 卡 32px 28px）；gallery grid gap 16px，2 列，≤640px 降 1 列（卡内 before→after 保持横向 nowrap）。
- 大区块节奏: hero 上边距 40px；gallery 与 footer 上边距 72px。宁空勿挤。
- 图片: 必须显式 width/height + loading=lazy；按显示宽 ≤170px，**源资产 340×340 WebP（显示宽 2×，SPEC-437 已落地：8 张共 ~84KB，512px PNG 4.45MB 已退役）**；新增 gallery 资产必须同规格，禁 512px+ PNG。

## 5. Motion

- 交互反馈仅 color / border-color / background 的 **0.15s** transition；primary hover 允许 `filter: brightness(1.06)`。
- 唯一持续动画 = loading spinner（2px 圆环 border rotate **0.9s linear infinite**）。`prefers-reduced-motion: reduce` 下已换非前庭信号：环形色不变、`spin-fade` opacity 脉冲 1.4s 保留「加载中」状态（SPEC-437 已落地）；**禁全局 0.01ms kill**。
- 加载诚实性: 只显示真实经过秒数（tabular-nums）+ 真实文案 hint 轮换；**禁止假百分比/假进度条**；job 有真实状态就显示真实状态。
- 禁止: fly-in 入场、bounce、parallax、骨架屏闪烁。

## 6. Icons & 切换控件语义（SPEC-436 固化硬规则）

- 图标一律 **inline 线条 SVG**: `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"`（56px 大占位用 1.2）round cap/join，装饰性一律 aria-hidden；颜色永远 currentColor 不写死。**禁 emoji、禁文本字符图标**（☾/☀ 已退役，线上零命中是验收项）。
- **切换类控件图标显示「目标态」**: dark 显示 sun（点击去 light）/ light 显示 moon（点击去 dark）；图标语义必须与 aria-label/title 一致（i18n key themeAriaToLight/themeAriaToDark）。参考源: icon-forge App.tsx:182/188（path 逐字）。
- themeBtn 静态初始 = sun SVG（与默认 dark 一致），消除 JS 加载后跳变；JS 侧 renderThemeBtn 用 innerHTML 换 SUN_SVG/MOON_SVG 常量（app.js:42-43）。
- 品牌 mark（circle r9.25 + 双圆角 rect 眼）在 brand / dropzone / 结果占位三处复用，是产品签名；**禁止单处私改**。

## 7. Anti-AI-slop checklist（violation = rejected）

1. ❌ 紫蓝渐变 / 「AI 紫」配色
2. ❌ Glassmorphism / backdrop-blur 滥用
3. ❌ 自我 congratulatory 文案 — botu 文案只陈述事实（免费/免注册/约 3-4 分钟/支持格式）
4. ❌ Emoji 当图标
5. ❌ 无意义 badge（「Beta」「Powered by」一律删）
6. ❌ 假进度百分比
7. ❌ 衬线字体 / 装饰斜体标题
8. ❌ 新增色相、新增圆角档、新增阴影档

## 8. Component baselines

- **pill**（顶栏圆按钮）: 透明底 + 1px --line 边框 + --muted 文本，hover 全转 --accent；focus-visible 2px accent outline + 2px offset；**移动端 ≤560px min-height/min-width 44px（inline-flex 居中，SPEC-437 已落地），桌面保持紧凑视觉**。
- **btn**: 透明底 + --line 边框 + --ink 文本，radius 8，padding 9px 22px；hover 转 accent；disabled opacity .45 + pointer-events none。
- **btn.primary**: --accent 底 + --accent-ink 字 + 600 字重；hover 仅 brightness(1.06)。
- **card**: --panel 底 + --line 边框 + --shadow + radius 12，text-align center。
- **dropzone**: card 变体，dashed 边框，52px 28px padding；hover/dragover = accent 边框 + accent 5% 底（color-mix）；role=button + tabindex=0 + Enter/Space 触发 fileInput。
- **errbox**: --err-bg/--err-ink + role=alert + word-break；**quota 行**: --accent 文本 + role=status，耗尽转 --accent2；两者默认 hidden，fail-open。
- **slot/结果框**: 内联 SVG 占位随主题 currentColor；**任何状态不得出现空 src 的 <img>**。
- **gallery 卡**: before→after 强制横向（nowrap），idx 用 --accent2 + tabular-nums，caption 居中 micro 档；img 340×340 WebP + width/height 属性 + lazy。
- **topbar**: brand 左（mark 22px + wordmark 1.05rem/600），pill 组右（theme+lang，gap 8px）。
- **footer**: border-top --line，三层 = SEO 文案(small) / 署名(footnote, --ink) / 版权(micro)。

## 9. 行为 & i18n 规则

- 主题: head 内联脚本 pre-apply `data-theme`（localStorage `botu-theme` → prefers-color-scheme → 默认 dark），零 flash；切换必写回 localStorage。
- 语言: data-i18n key 驱动；setLang 必须同步 `documentElement.lang`（zh-CN/en，i18n.js:123）；新增 key 必须双语齐落。
- 资产版本: **改 css/js/图片内容必 bump `?v=`**（zone browser_cache_ttl 覆盖 _headers，max-age=14400 实测；当前 **?v=437**，SPEC-436 内容变更滞后的 ≤4h 回归窗口已随 SPEC-437 bump 关闭）。
- quota/状态读取失败 fail-open 不阻断生成；错误只进 errBox + console，禁 alert/弹窗。
